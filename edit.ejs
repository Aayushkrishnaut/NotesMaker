<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="container mx-auto px-4 py-10 min-h-screen">
        <a class="text-blue-500 inline-block mb-10 hover:underline" href="/">Go back to home</a>
       
        <form action="/edit/<%= filename %>" method="post">
            <div class="mb-4">
                <label for="previous" class="block text-lg font-medium">Previous Name</label>
                <input id="previous" class="block px-3 py-2 w-full rounded-md text-white bg-zinc-800" type="text" value="<%= filename %>" disabled>
            </div>
            <div class="mb-4">
                <label for="new" class="block text-lg font-medium">New Name</label>
                <input id="new" class="block px-3 py-2 w-full rounded-md text-white bg-zinc-800" type="text" placeholder="new name" name="new" aria-label="New Name">
            </div>
            <div class="mb-4">
                <label for="filedata" class="block text-lg font-medium">File Content</label>
                <textarea id="filedata" class="block px-3 py-2 w-full rounded-md text-white bg-zinc-800" name="filedata" rows="10"><%= filedata %></textarea>
            </div>
            <input class="mt-2 bg-blue-500 px-5 py-2 rounded-md text-white cursor-pointer" type="submit" value="Update File">
        </form>

        <!-- Delete File Button -->
        <form action="/delete/<%= filename %>" method="post" onsubmit="return confirm('Are you sure you want to delete this file?');">
            <input class="mt-4 bg-red-500 px-5 py-2 rounded-md text-white cursor-pointer" type="submit" value="Delete File">
        </form>
    </div>
</body>
</html>
